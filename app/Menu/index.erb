<div class="pageTitle">
  <h1>Menus</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @menus.each do |menu| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => menu.object %>">
            <span class="title"><%= menu.id:integer %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>